const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('.site-nav');menuButton?.addEventListener('click',()=>{const open=nav.classList.toggle('is-open');menuButton.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.site-nav a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('is-open');menuButton?.setAttribute('aria-expanded','false')}));const slides=[...document.querySelectorAll('.hero-image')];const currentSlide=document.querySelector('.current-slide');let activeSlide=0;function showSlide(index){activeSlide=(index+slides.length)%slides.length;slides.forEach((slide,i)=>slide.classList.toggle('is-active',i===activeSlide));currentSlide.textContent=String(activeSlide+1).padStart(2,'0')}document.querySelector('.gallery-prev')?.addEventListener('click',()=>showSlide(activeSlide-1));document.querySelector('.gallery-next')?.addEventListener('click',()=>showSlide(activeSlide+1));document.querySelectorAll('.residence-toggle').forEach(button=>button.addEventListener('click',()=>{const card=button.closest('.residence-card');const wasOpen=card.classList.contains('open');document.querySelectorAll('.residence-card').forEach(item=>{item.classList.remove('open');item.querySelector('.residence-toggle').setAttribute('aria-expanded','false')});if(!wasOpen){card.classList.add('open');button.setAttribute('aria-expanded','true')}}));document.querySelector('.request-form')?.addEventListener('submit',event=>{event.preventDefault();const form=event.currentTarget;const name=form.elements.name.value.trim();const contact=form.elements.contact.value.trim();const goal=form.elements.goal.value.trim();const message=form.querySelector('.form-success');if(!name||!contact){message.textContent='Укажите имя и способ связи.';return}const text=['Здравствуйте! Хочу получить подбор по City Walk и Central Park.',`Имя: ${name}.`,`Контакт: ${contact}.`,goal?`Задача: ${goal}.`:''].filter(Boolean).join('\n');window.open(`https://wa.me/971508698020?text=${encodeURIComponent(text)}`,'_blank','noopener')});
